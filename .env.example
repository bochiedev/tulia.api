# Django Settings
SECRET_KEY=your-secret-key-here-change-in-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database
# For development (SQLite):
DATABASE_URL=sqlite:///db.sqlite3
# For production (PostgreSQL):
# DATABASE_URL=postgresql://tulia_user:tulia_pass@localhost:5432/tulia_db
DB_CONN_MAX_AGE=600

# Redis
REDIS_URL=redis://localhost:6379/0
CELERY_BROKER_URL=redis://localhost:6379/1

# Celery
CELERY_RESULT_BACKEND=redis://localhost:6379/2

# Twilio (per tenant, these are examples)
# TWILIO_ACCOUNT_SID=your-twilio-sid
# TWILIO_AUTH_TOKEN=your-twilio-token
# TWILIO_WHATSAPP_NUMBER=+14155238886

# WooCommerce (per tenant)
# WOO_STORE_URL=https://example.com
# WOO_API_KEY=ck_xxxxx
# WOO_API_SECRET=cs_xxxxx

# Shopify (per tenant)
# SHOPIFY_SHOP_DOMAIN=example.myshopify.com
# SHOPIFY_ACCESS_TOKEN=shpat_xxxxx

# OpenAI/Claude for Intent Classification
OPENAI_API_KEY=your-openai-key-here
# ANTHROPIC_API_KEY=your-claude-key-here

# Sentry Error Tracking
SENTRY_DSN=
SENTRY_ENVIRONMENT=development
# SENTRY_RELEASE=1.0.0  # Optional: Set to your app version for release tracking

# Encryption (generate with: python -c "import os, base64; print(base64.b64encode(os.urandom(32)).decode('utf-8'))")
ENCRYPTION_KEY=your-base64-encoded-32-byte-key-here

# Rate Limiting
RATE_LIMIT_ENABLED=True

# Logging
LOG_LEVEL=INFO
JSON_LOGS=False

# Email Configuration
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_HOST=localhost
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
DEFAULT_FROM_EMAIL=noreply@tulia.ai

# Frontend URL
FRONTEND_URL=http://localhost:3000

# Subscription
DEFAULT_TRIAL_DAYS=14
